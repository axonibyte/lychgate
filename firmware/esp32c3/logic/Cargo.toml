# Pure firmware logic with no HAL dependency, so it host-tests: the flash
# seq-record codec (two-slot ping-pong + CRC32). CI runs
# `cargo test -p lychgate-esp32c3-logic --target <host>` from the firmware
# workspace; the app crate consumes it on riscv.
[package]
name = "lychgate-esp32c3-logic"
version = "0.12.0"
edition = "2021"
license = "BSD-2-Clause"
publish = false

[dependencies]
